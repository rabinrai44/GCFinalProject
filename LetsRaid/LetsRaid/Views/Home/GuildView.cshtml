@model IEnumerable<LetsRaid.Models.GuildModels.Guild>

@{
    ViewBag.Title = "GuildView";
}

<h2>GuildView</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Server.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Server.Name)
        </td>
        <td>
            @Html.ActionLink(item.Name,"GetMembers","Character",new { model = item.GuildId })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
            @*@Html.ActionLink(item.Name, "Dalaran", "Home", new { Id = item.ServerId }, null)*@
        </td>
    </tr>
}

</table>
